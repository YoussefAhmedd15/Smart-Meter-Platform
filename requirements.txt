fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
pydantic>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
pyserial>=3.5,<4.0
requests>=2.31,<3.0

# Used by users.py and the data-engineering pipeline scripts (load_*.py,
# create_*_analytics.py, normalize_scdc.py, etc.) — previously imported but
# undeclared here. Also now used by backend/app/core/security.py (password
# hashing), which ports users.py's bcrypt logic rather than reintroducing it.
bcrypt>=4.0,<5.0
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0

# JWT access tokens for backend/app/core/security.py.
PyJWT>=2.8,<3.0

# Required by fastapi.testclient.TestClient (used in backend/tests/test_auth.py).
httpx>=0.27,<1.0

# Used by scripts/azure_client.py (Azure test-run/result reporting; optional,
# only needed if an external pytest suite imports that module).
# NOTE: PyPI's azure-devops package has no stable 7.1 release, only betas
# (its v7_1 module -- what this project imports -- first appeared in 7.1.0b1).
# Pinned exactly since pre-release versions won't satisfy a plain >=/< range.
azure-devops==7.1.0b4
msrest>=0.7,<1.0
